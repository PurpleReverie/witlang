# generated output — rebuild with:
#   node packages/cli/dist/bin.js build website/site.wit -o website/site.html --raw
site.html

# generated docs site (anchored so it does NOT match website/content/build/,
# the authored Build track)
/build/

# local deploy config (secrets/host-specific) — commit .env.example instead
.env

# generated landing-page config partial (emitted by gen-config.mjs from .env)
/parts/config.wit
