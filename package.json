{
  "name": "wit",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "test:tour": "pnpm build && node packages/cli/dist/bin.js tour --report tests/integration/feature-tour.wit",
    "test:parse": "pnpm build && node packages/cli/dist/bin.js parse tests/integration/feature-tour.wit",
    "typecheck": "tsc --build",
    "build": "tsc --build",
    "vscode:install": "pnpm --filter wit-vscode install:local"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
